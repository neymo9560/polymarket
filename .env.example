# Configuration Polymarket HFT Bot 2026
# Copier ce fichier en .env et modifier avec vos valeurs

# Clé privée Ethereum (format: 0x1234567890abcdef...)
# OBLIGATOIRE pour mode live, optionnel pour mode paper
POLY_PRIVATE_KEY=

# Mode de fonctionnement par défaut
# paper = simulation sans risque (recommandé pour débuter)
# live = trading réel avec frais (ATTENTION: argent réel)
BOT_MODE=paper

# Configuration fréquence scan (millisecondes)
# Plus bas = plus rapide mais plus CPU
SCAN_FREQUENCY=1000

# Gestion des risques
# Taille max position (% du balance)
MAX_POSITION_SIZE=0.03

# Montant max risque par trade (USDC)
MAX_RISK_PER_TRADE=5.0

# Seuil arbitrage binaire (gabagool style)
# YES Ask + NO Ask < threshold → profit garanti
ARB_THRESHOLD=0.985

# Seuil low-prob NO (swisstony style)
# YES price < threshold → buy NO massivement
LOW_PROB_THRESHOLD=0.025

# Seuil scalping drop (Theo4 style)
# Drop % pour déclencher scalping
SCALPING_DROP_THRESHOLD=0.30

# Configuration WebSocket (pour latence optimale)
ENABLE_WEBSOCKET=true
WS_URL=wss://ws.clob.polymarket.com

# Configuration API
CLOB_URL=https://clob.polymarket.com
GAMMA_API_URL=https://gamma-api.polymarket.com

# Configuration UI
UI_REFRESH_RATE=1000
MAX_LOG_LINES=100

# Logging
RUST_LOG=info

# Configuration avancée
MAX_CONCURRENT_ORDERS=10
SLIPPAGE_TOLERANCE=0.01
COOLDOWN_MS=500
